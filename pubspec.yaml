name: clean_arcitechture_edu
description: "A new flutter project for learn clean arctiechrure by weather app."
publish_to: 'none'

version: 0.0.1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:

  cupertino_icons: ^1.0.8
  dio: ^5.6.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # assets:
  #   - assets/images/